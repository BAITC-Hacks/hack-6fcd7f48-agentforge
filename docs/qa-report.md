# QA проход — 23.09.2026

Объект: расчёт `backend.pipeline`, API v1 и три CSV. Синтетические Parquet создаются внутри `tmp_path`; официальный тест пропускается, если данных нет в checkout.

## Найдено и исправлено

- **P2 — компоненты:** `summary.n_components` исключал изоляты. Граф с одним связным фрагментом и одним seed-изолятом давал 1 вместо 2. Backend исправлен на полный подсчёт; на официальном наборе 35 компонент, включая 19 изолятов. Упомянутые в ТЗ 16 — компоненты с рёбрами.
- **P2 — переполнение сумм:** две отдельные конечные транзакции по `1e308` давали `summary.sum_kzt=Infinity` и `node.in_kzt=Infinity`, что нарушало JSON контракт. Backend теперь отклоняет такие входы с понятным `ValueError`; регрессия проходит.

## Результат проверок

- `UV_CACHE_DIR=/tmp/uv-cache uv run pytest -q` — **24 passed**. Проверены все узлы, изоляты, граница depth=4 и ограничение confidence, неопределённое отношение seed, конечность чисел JSON, роли/evidence, входная схема и переполнение, int64 gid в CSV/API, детерминированность экспорта, согласованное переключение трёх CSV и неизменность API snapshot при внешнем пересчёте, маршруты и лимиты API.
- `UV_CACHE_DIR=/tmp/uv-cache uv run ruff check backend tests scripts` — **pass**.
- Официальный CLI: 2 248 узлов, 3 119 рёбер, 4 840 транзакций, 81 seed; 2 248 строк ролей, 91 кластер, 100 строк топа. Измеренный ранее полный запуск — 0.91 с по wall clock, расчёт — 0.448151 с. Для всех официальных узлов проверены допустимая роль, evidence длиной 1–200, конечные scores в [0,1], ограничения confidence для depth=4 и seed. Размеры, seed и внутренний оборот каждого кластера сверены с сырыми Parquet; топ сверён с полным порядком priority_score/gid. Повтор на переставленных официальных строках дал побайтово равные три CSV; весь snapshot сериализуется с `allow_nan=False`.

## Приёмка интеграции

Lead проверил отдельную чистую копию исходников без данных, окружений, `node_modules` и внешнего harness. Установка из lockfiles с пустыми uv/npm кешами, получение официального архива без аккаунта, 24 теста (3,04 с), Ruff и frontend lint/typecheck/build прошли. Полный CLI занял 0,99 с, внутренний расчёт — 0,510786 с.

Docker Compose собран из этой копии и прошёл проверку готовности. Реальные HTTP-ответы трёх CSV имеют точные схемы, 2 248 / 91 / 100 строк и побайтно совпадают с опубликованным снимком. Для всех трёх ссылок интерфейса получены события скачивания. Карточка, граф и числовые основания приоритета проверены на 1440 и 390 px; нет горизонтального переполнения и ошибок JavaScript. Контейнер без данных показывает понятную ошибку в интерфейсе. Более ранний проход также проверил изолят, границу, неизвестный gid, пустой фильтр, выбор на графе, приближение/вписывание и клавиатурный поиск.

## Проверка кода перед промежуточной публикацией

Независимый review backend не выявил блокирующих замечаний. Расчёт разделён на этапы построения графа, назначения ролей, приоритетов, сводок кластеров и публикации CSV. После рефакторинга JSON-снимок без `elapsed_seconds` и все три CSV побайтно совпали с эталоном до правки; 24 Python-теста и Ruff проходят.

На frontend исправлено перезаписывание ручного выбора первым узлом из запоздавшего ответа списка. В браузерном сценарии ответ списка был удержан локальным proxy: выбран `100000003115284100`, затем ответ отпущен; карточка осталась на выбранном узле при появлении первого приоритета `100000003684369100`. Неполная ссылка `?view=cluster` открывает обзор. Нормализация режимов также проверяется `npm test`; lint, typecheck, production build и Docker healthcheck прошли. Карточка вынесена в отдельный компонент, дублирующие предупреждения удалены. Раскладка графа была улучшена после этого браузерного прохода; её новая геометрия проверена отдельно ниже.

Чистый remote clone коммита `94cc8a8` проверен с пустыми uv/npm-кешами и без данных/окружений в checkout: dataset получен публичным загрузчиком и SHA-256 совпал; 24 Python tests, Ruff, frontend test/lint/typecheck/build, Docker Compose healthy и API-проверка трёх CSV прошли. Job GitHub Actions не запустился из-за billing lock организации/аккаунта, а не из-за ошибки кода. [Текущий статус Actions](https://github.com/BAITC-Hacks/hack-6fcd7f48-agentforge/actions/workflows/verify.yml).

## Проверка новой окрестности графа

`node --test frontend/src/graphLayout.test.ts` и `selection.test.ts`, `npm test`, lint, typecheck и production build — pass после смены геометрии и исправления выбора узла. Раскладка проверяет изолят, отсутствующий выбранный gid, входящие/исходящие/взаимные связи, петлю, 20 контрагентов по сумме прямых переводов, числовую сортировку gid, полный список, сохранность финансовых полей и детерминированные координаты. Для 61 исходящего контрагента ограничены ширина и минимальное расстояние между узлами.

Проверка новой геометрии на desktop 1440 px: узлы со связями 10/11 и 7/12, плотный граф 86/88 (сначала 21/23), 74/104 (сначала 21/27), изолированный узел 1/0; выбор узла, увеличение и подписи, выделение ребра 210 000 KZT/2 перевода, сброс фона и выбор нового узла. Панель сведений показывает полный gid, точную сумму до двух знаков и число переводов; подпись сообщает фактические количества. Действие «Показать всех» раскрывает уже загруженные записи в пределах лимита 120.

На мобильном экране шириной 390 px проверены отсутствие горизонтального переполнения при ширине страницы 375 px, поиск, переключение количества 21/23 → 86/88 → 21/23, вписывание графа и выделение ребра `100000008748914100` → `100000001072022100` с суммой 320 000 KZT и 1 переводом. Ошибок в console нет. Это не означает, что все подписи всегда разделены: в плотных областях возможны пересечения; большие обзорные графы стоит исследовать приближением и выбором отдельных узлов.
