# Критерии кейса и подтверждение

Матрица фиксирует локальные проверки и отдельную приёмку чистого remote clone на `94cc8a8`. GitHub Actions job не запустился из-за billing lock организации/аккаунта; актуальный статус — [GitHub Actions](https://github.com/BAITC-Hacks/hack-6fcd7f48-agentforge/actions/workflows/verify.yml), workflow — [verify.yml](../.github/workflows/verify.yml).

| Критерий | Реализация | Проверка | Доказательство | Статус |
|---|---|---|---|---|
| Один воспроизводимый расчёт от трёх Parquet до CSV, ≤5 минут | `scripts/fetch_data.py`; CLI `backend.pipeline`; атомарные CSV-снимки | Запустить полный CLI, проверить три схемы/строки, повторить и сравнить байты | 2 248 узлов, 3 119 рёбер, 4 840 транзакций; чистая копия: полный CLI 0,99 с, внутренний analyze 0,510786 с; CSV побайтно совпали | PASS в чистой локальной копии |
| Роль, score и evidence для каждого узла | Шесть ролей; score в [0,1] с cap seed/boundary; evidence до 200 символов | 24 pytest за 3,04 с в чистой копии; API smoke; сверить строку и explanation | 2 248 узлов; no NaN; `pass_through` null у seed; evidence максимум 122 символа на наборе | PASS |
| Формальные объяснимые правила всех ролей | Упорядоченные правила в `backend/pipeline.py`, обоснование использует метрики | Тесты правил, boundary/seed и проверка трёх примеров в GUI | 24 pytest; desktop/mobile сценарий; примеры подробно приведены в [методологии](methodology.md) и README | PASS для требований кейса |
| Кластеризация с размером, seed, оборотом и гипотезой | Louvain, сводная строка `clusters.csv` с русской гипотезой и числом кандидатов ролей | Проверить покрытие, поля, обороты и текст для изолятов | 91 кластер; 8 кластеров с более чем одним seed; pytest и API smoke PASS | PASS |
| Топ ≥20 приоритетных узлов с объяснением | Ранжирование и `why`: evidence, оборот, степень, PageRank и формула | Проверить порядок, длину топ-листа и значения причины; в карточке открыть расчёт приоритета | CLI/API smoke и повторное побайтное сравнение CSV PASS | PASS |
| Экран: направленная сеть, роли, поиск `gid`, карточка и кластер | React/Cytoscape; отдельные зоны входящих, исходящих и взаимных связей; 20 крупнейших контрагентов, раскрытие всей доступной окрестности, выбор перевода | Браузер на 1440 и 390 px: малые и плотные окрестности, изолят, поиск, масштаб, выбор узлов и связей; [сценарии и результаты](qa-report.md#проверка-новой-окрестности-графа) | Количества совпадают с отображением; полные gid, сумма и число операций доступны при выборе связи. Тесты раскладки/выбора, lint, typecheck и build PASS; нет горизонтального переполнения страницы и ошибок JavaScript | PASS для перечисленных сценариев; большие обзорные графы требуют приближения |
| Скачивание CSV из API и браузера | Allowlist трёх endpoint в `backend.app`; отдача с attachment headers | HTTP запросы и browser download event для каждого файла; строки, заголовки и байты сверены со снимком | В чистой копии все endpoints вернули 200; CSV содержат 2 248 / 91 / 100 строк, байты совпали с `out`; браузер скачал все три | PASS |
| Учитывать границу данных и не делать вывод о виновности | Caps для seed/boundary, предупреждения в карточке и объяснении | Проверить известный boundary и seed; неизвестный узел и отсутствие входа | API smoke + browser boundary scenario PASS; 444 boundary в summary | PASS |
| Локальный runtime без платного API/аккаунта | Локальный расчёт; fetch из открытой ссылки или локального ZIP | Запуск без ключей; проверить locked installation и отсутствие внешних calls | `fetch_data.py` не требует cookies/account; runtime без моделей/API; audit frontend 0 vulnerabilities | PASS для текущей локальной установки |
| Docker/Compose | Один multi-stage image/service; data read-only, out write; loopback port | В чистой копии `docker compose up --build -d --wait`; проверить health и приложение в браузере | Compose build/up --wait PASS и healthy; browser 1440/390 PASS через `127.0.0.1:8000`, нет overflow и console errors | PASS |
| Числовой overflow при агрегации экстремальных значений | Проверка finite totals до построения графа | Regression test с суммами порядка `1e308` | Числовые totals не могут молча превратиться в Infinity; pipeline выдаёт понятный `ValueError`; 24 теста PASS | PASS |
| Чистый remote clone | Те же инструкции и lockfiles; загрузчик получает официальный dataset | Fresh clone коммита `94cc8a8`, пустые uv/npm caches, download данных, frontend/backend проверки, Docker и API/CSV | 24 Python tests, Ruff, frontend checks, Docker healthy и HTTP 200 трёх CSV прошли | PASS для `94cc8a8` |
| CI workflow | Репозиторий и workflow [verify.yml](../.github/workflows/verify.yml) | Проверить запуск в [GitHub Actions](https://github.com/BAITC-Hacks/hack-6fcd7f48-agentforge/actions/workflows/verify.yml) | Job не начал выполнение из-за billing lock организации/аккаунта, это не ошибка тестов или сборки | Не проверено: workflow не стартовал |
| Описать направление масштаба ~1 млн узлов | План технических изменений без заявления о достигнутом масштабе | Сверить в README и methodology | [Методология](methodology.md) | PASS: план масштабирования документирован; реализация миллионного графа не заявлена |

## Вес критериев

Соответствие задаче и работоспособность — 25 баллов; техническая реализация — 25; README и воспроизводимость — 25; ценность и применимость — 15; потенциал развития и оригинальность — 10. Это веса из задания, не оценка команды.
